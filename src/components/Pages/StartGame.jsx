function StartGame(){
    return (
        <>        {/* React Fragments ==> we can write comment like this in JSX*/}
            <h1>Start Game</h1>
        </>
        
    )
}
//  NOTE==> if from , components we are returning something JSX  it can return only single parent,==> inside that parent i can use anything which is also nested

export default StartGame;